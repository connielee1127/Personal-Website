specVersion: "v1beta"
serviceId: "cblog_folder"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cblog_folder-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
